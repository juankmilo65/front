import styled from 'styled-components';

const Pointer = styled.div`
  cursor: pointer;
`;

const WrapperActions = styled.span`
  display: flex;
  justify-content: space-evenly;
`;

export { Pointer, WrapperActions }